mission:
  name: "Excavation run DIG"
  steps:
  # Deeper dig
  - action: actuator_position
    params:
      target: dig2
      timeout_s: 8
  # Digging
  - action: drive_backward
    params:
      distance_m: 0.75
      use_camera: false
      timeout_s: 15
  # Skimming pose
  - action: actuator_position
    params:
      target: dig1
      timeout_s: 8
  # Diggging 
  - action: drive_backward
    params:
      distance_m: 0.75
      use_camera: false
      timeout_s: 15
  # Set pose to drive
  - action: actuator_position
    params:
      target: drive
      timeout_s: 8
  # End
  - action: stop
